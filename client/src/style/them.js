const size = {
  mobile: '800px',
  avatar: '1100px',
};

const theme = {
  // mainColor: '#0a4297',
  mobile: `(max-width: ${size.mobile})`,
  avatar: `(max-width: ${size.avatar})`,
};

export default theme;
